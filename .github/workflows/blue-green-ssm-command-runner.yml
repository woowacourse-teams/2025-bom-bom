name: Backend Blue-Green Production Deploy (SSM)

on:
  push:
    branches:
      - server-new
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-northeast-2
  ROLE_ARN: arn:aws:iam::<AWS_ACCOUNT_ID>:role/GithubActions-BomBom-Deploy-Role

  PROD1_ID: i-043b2198bd0564f99
  PROD2_ID: i-02583d84fc5e0c678
  LB_TARGET: i-06856239dadc6d1fa

  COMPOSE_FILE: docker-compose-prod-sub.yml
  HEALTH_URL: http://localhost:8081/actuator/health

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.mk.outputs.release_id }}
    steps:
      - id: mk
        run: echo "release_id=$(date +%Y%m%d-%H%M%S)-${GITHUB_SHA::12}" >> $GITHUB_OUTPUT

  build:
    runs-on: ubuntu-latest
    needs: prepare
    defaults:
      run:
        working-directory: ./backend/bom-bom-server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: gradle

      - name: Create application.yml
        run: |
          mkdir -p src/main/resources
          printf '%s' "${{ secrets.APPLICATION_PROD_PROPERTIES }}" > src/main/resources/application.yml

      - name: Build JAR
        run: ./gradlew clean assemble --no-daemon --stacktrace --info

      - uses: actions/upload-artifact@v4
        with:
          name: bom-bom-jar
          path: ./backend/bom-bom-server/build/libs/*.jar

  test:
    runs-on: ubuntu-latest
    needs: build
    defaults:
      run:
        working-directory: ./backend/bom-bom-server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Create test application.yml
        run: |
          mkdir -p src/test/resources
          printf '%s' "${{ secrets.APPLICATION_TEST_PROPERTIES }}" > src/test/resources/application.yml

      - name: Run Tests
        run: ./gradlew test --no-daemon --stacktrace --info

  docker-image:
    runs-on: ubuntu-latest
    needs: [build, test]
    defaults:
      run:
        working-directory: ./backend/bom-bom-server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: bom-bom-jar
          path: ./backend/bom-bom-server/build/libs

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push Image
        run: |
          docker buildx build \
            --platform linux/arm64,linux/amd64 \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/bom-bom:prod \
            --push .

  # -----------------------------------------------------------
  # 1) PROD2 (Blue) 배포
  # -----------------------------------------------------------
  deploy-prod2:
    name: "🚀 Step 1: Deploy Prod2 (Blue)"
    needs: docker-image
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Deploy via SSM
        run: |
          aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "Key=InstanceIds,Values=${{ env.PROD2_ID }}" \
            --parameters commands='[
              "cd /opt/bom-bom",
              "sudo docker compose -f '${{ env.COMPOSE_FILE }}' down || true",
              "sudo docker compose -f '${{ env.COMPOSE_FILE }}' pull",
              "sudo docker compose -f '${{ env.COMPOSE_FILE }}' up -d --remove-orphans --force-recreate"
            ]' \
            --comment "Deploy prod2"

  # -----------------------------------------------------------
  # 2) PROD2 헬스체크
  # -----------------------------------------------------------
  healthcheck-prod2:
    name: "🩺 Step 2: Health Check Prod2"
    needs: deploy-prod2
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Health Check
        run: |
          for i in {1..30}; do
            CMD=$(aws ssm send-command \
              --document-name "AWS-RunShellScript" \
              --targets "Key=InstanceIds,Values=${{ env.PROD2_ID }}" \
              --parameters commands='["curl -o /dev/null -s -w \"%{http_code}\" '${{ env.HEALTH_URL }}'"]' \
              --query "Command.CommandId" \
              --output text)

            sleep 3

            RES=$(aws ssm get-command-invocation \
              --command-id "$CMD" \
              --instance-id "${{ env.PROD2_ID }}" \
              --query "StandardOutputContent" \
              --output text || echo "000")

            echo "STATUS: $RES"

            if [ "$RES" = "200" ]; then exit 0; fi
            sleep 5
          done

          echo "❌ Health check failed"
          exit 1

  # -----------------------------------------------------------
  # 3) HAProxy 트래픽 PROD2로 스위칭
  # -----------------------------------------------------------
  shift-prod2-live:
    name: "🔀 Step 3: Switch traffic to prod2"
    needs: healthcheck-prod2
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Switch HAProxy
        run: |
          aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "Key=InstanceIds,Values=${{ env.LB_TARGET }}" \
            --parameters commands='[
              "echo \"disable server app_servers/prod1\" | sudo socat stdio /var/run/haproxy.sock",
              "echo \"enable server app_servers/prod2\" | sudo socat stdio /var/run/haproxy.sock"
            ]'

  # -----------------------------------------------------------
  # 4) PROD1 배포
  # -----------------------------------------------------------
  deploy-prod1:
    name: "🚀 Step 4: Deploy Prod1 (Green)"
    needs: shift-prod2-live
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Deploy via SSM
        run: |
          aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "Key=InstanceIds,Values=${{ env.PROD1_ID }}" \
            --parameters commands='[
              "cd /opt/bom-bom",
              "sudo docker compose -f '${{ env.COMPOSE_FILE }}' down || true",
              "sudo docker compose -f '${{ env.COMPOSE_FILE }}' pull",
              "sudo docker compose -f '${{ env.COMPOSE_FILE }}' up -d --remove-orphans --force-recreate"
            ]'

  # -----------------------------------------------------------
  # 5) PROD1 헬스체크
  # -----------------------------------------------------------
  healthcheck-prod1:
    name: "🩺 Step 5: Health Check Prod1"
    needs: deploy-prod1
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Health Check
        run: |
          for i in {1..30}; do
            CMD=$(aws ssm send-command \
              --document-name "AWS-RunShellScript" \
              --targets "Key=InstanceIds,Values=${{ env.PROD1_ID }}" \
              --parameters commands='["curl -o /dev/null -s -w \"%{http_code}\" '${{ env.HEALTH_URL }}'"]' \
              --query "Command.CommandId" \
              --output text)

            sleep 3

            RES=$(aws ssm get-command-invocation \
              --command-id "$CMD" \
              --instance-id "${{ env.PROD1_ID }}" \
              --query "StandardOutputContent" \
              --output text || echo "000")

            if [ "$RES" = "200" ]; then exit 0; fi
            sleep 5
          done

          echo "❌ Health check failed"
          exit 1

  # -----------------------------------------------------------
  # 6) Traffic Restore (prod1 + prod2 enable)
  # -----------------------------------------------------------
  switch-final:
    name: "🔁 Step 6: Restore traffic (prod1 + prod2)"
    needs: healthcheck-prod1
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Restore HAProxy
        run: |
          aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "Key=InstanceIds,Values=${{ env.LB_TARGET }}" \
            --parameters commands='[
              "echo \"enable server app_servers/prod1\" | sudo socat stdio /var/run/haproxy.sock",
              "echo \"enable server app_servers/prod2\" | sudo socat stdio /var/run/haproxy.sock"
            ]'
